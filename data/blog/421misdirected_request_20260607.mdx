---
title: '421 Misdirected Request で閲覧不能になった ― Apache 2.4 の SNI 厳格化と ALB 構成'
date: '2026-06-07'
tags: ['Apache', 'AWS', 'ALB', 'SNI', 'troubleshooting']
draft: false
summary: 'ALB + EC2(Apache) + ACM で運用していたサイトが、ある日突然 421 Misdirected Request で閲覧不能に。原因は Apache 2.4.64 の SNI チェック厳格化。事象・原因・解決策を出典付きで整理する。'
---

## サマリ

ALB + EC2(Apache) + ACM で運用していたコーポレートサイトが、ある日突然 `421 Misdirected Request` で閲覧不能になった。原因は Apache のアップデート(2.4.64)による SNI チェックの厳格化。最終的に Apache の VirtualHost 設定の見直しと、構成自体の変更(ACM → Certbot、ALB 経由 → EC2 直)で解消した。

この記事では、事象・421 とは何か・原因・解決策を、出典付きで整理する。

## 事象

- **環境**: 本番環境
- **構成**: ALB + EC2(Apache) + ACM
- **症状**: サイトにアクセスすると `421 Misdirected Request` が表示され、閲覧できない

ブラウザに出るメッセージ:

```text
Misdirected Request
The client needs a new connection for this request as the requested host name
does not match the Server Name Indication (SNI) in use for this connection.
```

平常時は問題なく閲覧できていたサイトが、サーバ側で特に変更を加えていないにもかかわらず、ある日を境に閲覧不能になった。

## 421 Misdirected Request とは何か

### 定義

`421 Misdirected Request` は HTTP のクライアントエラーステータスコードのひとつ。**リクエストが、それに対する応答を生成できないサーバに送られた**ことを示す[^mdn]。

具体的には、リクエスト URI に含まれる scheme(http/https)と authority(ホスト名)の組み合わせに対して、サーバが応答するように構成されていない場合に返される[^mdn]。クライアントは別の接続でリクエストを再試行してよい、とされている。仕様上の定義は RFC 9110(HTTP Semantics)にある[^rfc]。

### どんな時に起きるのか

典型的なのは、**接続の再利用(connection reuse)と SNI が絡む場面**だ[^mdn]。

- 1つの IP アドレスで複数のドメインをホストしている(SNI でドメインを出し分けている)
- ワイルドカード証明書(`*.example.com`)で、1つの接続を `abc.example.com` と `def.example.com` で使い回す
- このとき、サーバがドメインの切り替えを正しく処理できないと、421 を返す

ここで鍵になるのが SNI(Server Name Indication)という仕組みだ。

### SNI(Server Name Indication)とは

SNI は TLS プロトコルの拡張で、**クライアントが TLS ハンドシェイクの最初の段階で「どのホスト名に接続したいか」をサーバに伝える**仕組み[^sni]。

これにより、1つの IP アドレスで複数のドメインをホストし、ドメインごとに異なる SSL 証明書を返すことができる。SNI がなければ、サーバはどの証明書を返せばいいか判断できない。

つまり 421 は、ざっくり言うと「**TLS ハンドシェイクで伝えられたホスト名(SNI)と、実際に来た HTTP リクエストのホスト名(Host ヘッダー)が食い違っている**」ときに、サーバが「このリクエストは私宛じゃない」と突き返す状態だ。

## 原因

### Apache 2.4.64 で SNI チェックが厳格化された

調べたところ、**Apache HTTP Server 2.4.64(2025年7月10日リリース)で、SNI の取り扱いが厳格化された**ことが原因だった[^deflect]。

具体的には、この更新により Apache は、**TLS ハンドシェイクの SNI 名と、HTTP パケットの Host ヘッダーが一致しない接続を拒否するようになった**[^deflect]。

Apache のログには、次のようなメッセージが出る[^bitninja]:

```text
AH02032: Hostname (default host as no SNI was provided) and hostname
www.example.com provided via HTTP have no compatible SSL setup
```

### なぜ ALB 構成で多発したのか

この問題は、**ロードバランサーやリバースプロキシを挟む構成**で多発した。理由は、**多くのプロキシがバックエンドへの接続時に SNI を転送しない**から。

AWS の ALB / ELB も同様で、**ALB はバックエンド(Apache)への接続時に、元のリクエストの SNI を転送しない**[^awsrepost]。そのため、Apache から見ると「SNI が来ていない、または Host ヘッダーと一致しない」状態になり、2.4.64 以降の厳格化されたチェックに引っかかって 421 を返していた。

平常時は問題なく動いていたのに突然壊れたのは、サーバ側の設定を変えたからではなく、**OS のパッケージ更新で Apache が 2.4.64 に上がり、それまで「たまたま動いていた」設定が厳格化によって弾かれるようになった**ためだった。

> **補足: CVE との関係について**
>
> この SNI 厳格化を「CVE-2024-38474 / 38475 / 38476 への対応」と説明している記事が散見されるが、これらの CVE は mod_rewrite / mod_proxy のパス処理に関する別件であり、SNI 厳格化とは直接の関係はない[^apachevuln]。2.4.64 は SSL/TLS まわりの複数の修正を含むリリースであり、SNI と Host ヘッダーの整合性チェック強化はその一部である。二次情報の CVE 紐付けはやや不正確なので、原因は「2.4.64 での SNI 取り扱いの厳格化」と理解するのが正しい。

## 解決策

### 一般的な対処の選択肢

この事象への対処は、大きく次のいずれかになる。

**選択肢1: ロードバランサーで SSL を終端し、バックエンドへは HTTP で転送する**

ALB 側で HTTPS を終端してしまえば、Apache へは HTTP(80)で転送されるため、Apache 側で SNI 不一致が発生しなくなる。証明書も ALB 側に集約でき、管理負担が減る。最も推奨される構成とされている[^saizou]。

```text
クライアント → ALB(443, SSL終端) → Apache(80, HTTP)
```

**選択肢2: `SSLStrictSNIVHostCheck off` を設定する**

Apache 側で SNI の厳格チェックを無効化する設定。メイン設定と各 VirtualHost に `SSLStrictSNIVHostCheck off` を追加する[^awsrepost2]。ただし、これは厳格化された挙動をオフにするものなので、根本対応というより緩和策。

**選択肢3: Apache をバージョンダウンする**

2.4.63 以前に戻せば回避できるが、**セキュリティ修正を取り消すことになるため、あくまで暫定対応**[^saizou]。再度パッケージ更新が走ると再発する。

### 今回実際に行った対応

**1. デフォルト VirtualHost の設定見直し**

デフォルトの VirtualHost に、該当サイトのドキュメントルート等を設定し、SNI が提供されない/一致しないリクエストでも正しく処理されるようにした。

**2. ネームサーバー(DNS)の向き先変更**

元々ネームサーバーは Route 53 ではなく Value Domain のサーバを利用していた。Value Domain 側の DNS レコードの向き先を、ALB から EC2 直に変更した。これにより ALB を経由しない構成にし、SNI 転送の問題自体を回避した。

**3. 証明書を ACM から Certbot に切り替え**

ALB を経由しない構成にしたため、ACM(ALB にアタッチして使う)は使えなくなる。代わりに EC2 上で Certbot(Let's Encrypt)を使って証明書を取得・更新する構成にした。

### 結果

- サイトが正常に閲覧できるようになった
- ALB を使わなくなったことで、**サーバ代が月約3,000円削減**された

## 学び・次回への教訓

**ミドルウェアの自動アップデートや仕様変更により、既存の設定が意図せず無効化されるリスクがある。**

今回の事象の本質は、「サーバ側で何も変えていないのに壊れた」ように見えて、実際には **OS のパッケージ更新で Apache が 2.4.64 に上がり、セキュリティ厳格化によって"たまたま動いていた"設定が弾かれた**というものだった。

教訓としては:

- **リリースノートの確認**: 特に SSL/TLS まわりの挙動変更は、構成全体に影響しうる
- **デフォルト設定のケア**: SNI が提供されないケースに備え、デフォルト VirtualHost を適切に設定しておく
- **構成のシンプル化**: 今回のように小規模サイトであれば、ALB を挟まず EC2 直 + Certbot のほうが、SNI 転送問題を避けられてコストも下がる場合がある

「動いていたものが、自分が触っていないのに壊れた」ときは、**裏で何かが更新されていないか**を真っ先に疑うべき、という当たり前の話でもある。

[^mdn]: [421 Misdirected Request - HTTP | MDN](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status/421)
[^rfc]: [RFC 9110: HTTP Semantics - 421 Misdirected Request](https://www.rfc-editor.org/rfc/rfc9110#name-421-misdirected-request)
[^sni]: [SNIとは？TLS Server Name Indicationの仕組み | Cloudflare](https://www.cloudflare.com/ja-jp/learning/ssl/what-is-sni/)
[^deflect]: [Troubleshooting SNI and 421 Misdirected Request - Deflect](https://deflect.ca/docs/troubleshooting-sni-and-421-misdirected-request/)
[^bitninja]: [The Latest Apache Update: A 421 Misdirected Request Crisis - BitNinja Security](https://bitninja.com/blog/the-latest-apache-update-a-421-misdirected-request-crisis/)
[^awsrepost]: [Misdirected Request ... Apache 2.4.52 - AWS re:Post](https://repost.aws/questions/QUo5zj15ydRQS0yRLU8JKd2w/misdirected-request-the-client-needs-a-new-connection-for-this-request-as-the-requested-host-name-does-not-match-the-server-name-indication-sni-in-use-for-this-connection-apache-2-4-52)
[^awsrepost2]: [Apache 2.4.46 & ELB - AWS re:Post](https://repost.aws/questions/QUbWv0O9GoQ1yN1a9apWfH7Q/apache-2-4-46-elb)
[^saizou]: [ALB→Apache443 の構成で 421 Misdirected Request が発生する理由 - Zenn(saizou)](https://zenn.dev/saizou/articles/421-20251207)
[^apachevuln]: [Apache HTTP Server 2.4 vulnerabilities - The Apache HTTP Server Project](https://httpd.apache.org/security/vulnerabilities_24.html)
